#include "combat.h"
#include "player.h"
#include "enemy.h"

// Example function to process player attacks on enemies
void process_player_attack(Player* player, Enemy* enemy) {
    // Determine if player's attack hits enemy
    // Could be expanded with collision detection, etc.
}

// Example function to handle enemy attacks on the player
void process_enemy_attack(Player* player, Enemy* enemy) {
    // Apply damage if enemy hits player
}

